{
  "name": "buffer-graph",
  "description": "Resolve a dependency graph for buffer creation",
  "repository": "yoshuawuyts/buffer-graph",
  "version": "3.0.1",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps && node test"
  },
  "dependencies": {
    "blakejs": "^1.1.0"
  },
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "spok": "^0.8.1",
    "standard": "^10.0.2",
    "tape": "^4.7.0"
  },
  "keywords": [
    "graph",
    "dependency",
    "resolution"
  ],
  "license": "MIT"
}
